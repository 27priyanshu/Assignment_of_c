#include<stdio.h>

int main() {
    int length , breadth ;
    printf("what is length of rectangle\n");
    scanf("%d", &length);

    printf("what is the breadth of the rctangle\n");
    scanf("%d",&breadth);

    printf("area of the your reactangle is %d",length*breadth);
    
    return 0;
}