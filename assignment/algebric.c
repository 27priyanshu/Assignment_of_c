#include<stdio.h>

int main() {
    float a,b,x;
    float s;
    printf("enter the value of a, b , x \n");
    scanf("%f" "%f" "%f",&a,&b,&x);
    s=(a*x+b)/(a*x-b);
    printf("evalutaion of your algebric expression is %f",s);
    
    return 0;
}